import type HttpClient from './HttpClient';

/* eslint-disable-next-line @typescript-eslint/no-unused-vars */
export function extendHttpClient(httpClient: HttpClient): void {
    // By default nothing to extend
}
